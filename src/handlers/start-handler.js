import {START_ID} from '../../data/constants.js';
import { int } from '../data/constants.js';
export const startHandler = () => {
    const document.getElementById(START_ID)
        if(int!==null){
        clearInterval(int);
        }
        int = setInterval(mainTime,10);
};